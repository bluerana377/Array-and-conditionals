// Number Type variable
var sunglassPrice = 300;

// String type variable
var specialOneName = 'hande ercel';

// boolean type variable
var isSingle = true;
// two type of number: Integer: 120,
// float decimal: 3.65

// variable name convention: rules. best practice

/**
 * single word
 * no gap ore space
 * no quote 
 * no keyword
 * can not start with a number
 * can not contain dash
 * should use camelCase 
*/

// Math operation: +, -, *, /
//Shorthand: +=, -=, ++, --

// convert: parseInt, parseFloat

// to fixed

// reminder 'vagses er jonneya' %
 